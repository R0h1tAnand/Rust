//Constants or global variables
const NUMBER: i32 = 17; 
fn main() {
    // Variable are immutable
    let hello = "Hello, world!";
    //let mut hello = "Hello, World!";
    println!("{}", hello);
    let x = 5;
    let y = 6;
    println!("Sum of x: {} + y:{} is - {}", x, y, x + y);
    println!("{}", NUMBER);
}
